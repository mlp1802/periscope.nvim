- [ ]  Task A, sort pickers
The pickers in ./lua/periscope/pickers.lua can select files and tasks..however as they are right now, the sorting is unpredictable..its a mix between usage and fuzzy search score..the sorting should be ONLY based on usage (for file and tasks)..if fuzzy search has a zero score, the task or file should be hidden from the picker.

